
- hide out of stock items
* Limit max order items by stock
* edit cart with ajax,
* cart with ajax, delete
* show cart total with ajax above checkout button
* combine duplicate order_items
- add authorization
- search
- pagination
- add a couple sort/ scopes
  -recent products
  - price low
  - price high

- Sale items
- user profile with saved albums
- style
  - navbar within cart
- display sales
- add readme
- deploy

<div class="media album-cover" id="album-<%= album.id%>">
  <div class="media-left media-top">
    <%= link_to album_path(album), remote: true do %>
      <%= image_tag album.img_src, class: "media-object", size: "100x100" %>
    <% end %>
  </div>
  <div class="media-body">
    <h4 class="media-heading">
      <%= album.title %> - <%= album.artist%>
    </h4>
    <p id="item-quantity">
      In Stock: <%= album.quantity %> -
      In Cart:
      <span class="cart-quantity">
        <%= render partial: "layouts/cart_quantity", locals: {cart: cart, album_id: album.id} %>
      </span>
    </p>
    <% if current_user %>
      <%= form_for [cart, @order_item], remote: true do |f| %>
        <%= f.hidden_field :album_id, value: album.id %>
        <%= f.number_field :quantity %>
        <%= f.submit "Add to cart", class: 'add-to-cart' %>
      <% end %>
    <% end %>
  </div>
</div>
